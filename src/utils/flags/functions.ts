// import store from "redux/store";

// export const getFlag = (flagName: string, level: "org" | "cluster") => {
//   const redux = store.getState();
//   const { auth, cluster } = redux;
//   if (!auth.isLoggedIn || !auth.user) return null;
//   const org = auth.user.org_id;
// };
